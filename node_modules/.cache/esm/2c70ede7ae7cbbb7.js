let fs,existsSync;_ae7‍.w("fs/promises",[["default",["fs"],function(v){fs=v}]]);_ae7‍.w("fs",[["existsSync",["existsSync"],function(v){existsSync=v}]]);


async function load() {
    const data = await fs.readFile('data.json');
    const str = data.toString();
    return JSON.parse(str);
}  

async function save(data) {
    const stringified = JSON.stringify(data);
    await fs.writeFile('./data.json', stringified);
}

async function main() {
    const exists = existsSync('/data.json');
    
    if (exists) {
        const data = await load ();
        _ae7‍.g.console.log(data);
    } else {
        const data = {
            name: 'Katy Millard',
            age: 41,
            gender: 'female'
        }
        await save(data);
        console.log('New data saved.');
    }
}

main();